# Recommended Next Actions

1. Keep Policy C as the conservative report baseline and Policy A as the performance candidate; this audit does not change ranking or gates.
2. Treat the current Phase 7 VSP files as AVL-parity/body-axis geometry. For manual cruise inspection, use the new `cruise_normalized` variants.
3. Do not use VSPAERO CDo/CDtot as the mission profile drag number unless a traced viscous/profile setup is added. Keep the full-alpha/XFOIL profile budget as authority.
4. Smooth the chord schedule before presentation/manufacturing review. The monotone alternatives remove the mid/outer chord bump while preserving span and area to numerical precision.
5. If the manual inspection target is VSPAERO alpha=0 specifically, create a separate VSPAERO-calibrated incidence copy; keep the current cruise-normalized files as AVL-calibrated sidecar-parity variants.
6. Before changing ranking or gates, run one controlled comparison: AVL sidecar body-axis vs cruise-normalized vs monotone-chord, with the same references and full-polar profile-drag query layer.
