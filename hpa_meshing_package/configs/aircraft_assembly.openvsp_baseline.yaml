component: aircraft_assembly
geometry: ../data/blackcat_004_origin.vsp3
geometry_provider: openvsp_surface_intersection
out_dir: .tmp/runs/blackcat_004_baseline
units: m
mesh_dim: 3

farfield:
  enabled: true
  upstream_factor: 5.0
  downstream_factor: 12.0
  lateral_factor: 8.0
  vertical_factor: 8.0

su2:
  enabled: true
  case_name: alpha_0_baseline
  alpha_deg: 0.0
  max_iterations: 50
  reference_mode: auto
